export type CategoryFilterI = {
    category_id: number;
    name: string;
};

export type CarFilterI = {
    car_id: number;
    car: string;
};

export type TireFilterI = {
    tire_id: number;
    type: string;
};

export type WeatherFilterI = {
    weather: string;
};
